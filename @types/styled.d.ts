import "styled-components";

declare module "styled-components" {
  export interface DefaultTheme {
    colors: {
      primary: string;
      background: string;
      primary: string;
      secondary: string;
      font: string;
      heading: string;
    };
  }
}
